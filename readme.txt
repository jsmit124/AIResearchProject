To run our scripts open your terminal and go into the project directory. Then run a script by inputting the command:
python 'name_of_script.py'
where name_of_script is replaced by the actual name of the script and excluding the single quotes.
The resulst will be displayed to the terminal.